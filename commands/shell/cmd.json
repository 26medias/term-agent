[
    {
        "name": "execute_shell_command",
        "description": "Execute an arbitrary shell command",
        "parameters": {
            "type": "object",
            "properties": {
                "command": {
                    "type": "string"
                }
            },
            "required": ["command"]
        }
    }
]